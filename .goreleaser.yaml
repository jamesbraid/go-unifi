---
version: 2

# No generate hook: regeneration needs the retained controller artifact,
# which release runners do not hold. The committed generated output is
# digest-gated by CI; the release only packages it.

builds:
  - skip: true

changelog:
  use: github
  sort: asc
  abbrev: 0
  groups:
    - title: Features
      regexp: '^.*?feat(\([[:word:]]+\))??!?:.+$'
      order: 0
    - title: "Bug Fixes"
      regexp: '^.*?fix(\([[:word:]]+\))??!?:.+$'
      order: 1
    - title: "Performance Improvements"
      regexp: '^.*?perf(\([[:word:]]+\))??!?:.+$'
      order: 2
    - title: "Refactors"
      regexp: '^.*?refactor(\([[:word:]]+\))??!?:.+$'
      order: 3
    - title: "Documentation"
      regexp: '^.*?docs(\([[:word:]]+\))??!?:.+$'
      order: 4
    - title: "Build & CI"
      regexp: '^.*?(build|ci)(\([[:word:]]+\))??!?:.+$'
      order: 5
    - title: Other Work
      order: 999
  filters:
    exclude:
      - "^test:"
      - "^chore:"
      - "merge conflict"
      - Merge pull request
      - Merge remote-tracking branch
      - Merge branch
      - go mod tidy

release:
  # No github owner/name: goreleaser infers the target repo from the git
  # remote, so the release lands wherever the workflow runs rather than a
  # hardcoded upstream the fork's token cannot write to.
  draft: false
  prerelease: auto
  mode: append
  header: |
    ## UniFi Go Client {{ .Tag }} ({{ .Date }})

    Generated and tested against UniFi Network {{ .Env.CONTROLLER_VERSION }}.
  footer: |
    ## Installation

    To use this version in your Go project:

    ```bash
    go get github.com/ubiquiti-community/go-unifi@{{ .Tag }}
    ```

    **Full Changelog**: https://github.com/{{ .Env.GITHUB_REPOSITORY }}/compare/{{ .PreviousTag }}...{{ .Tag }}
  name_template: "{{ .Tag }}"
