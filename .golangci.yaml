version: "2"
issues:
  max-same-issues: 0
linters:
  enable:
    - durationcheck
    - copyloopvar
    - canonicalheader
    - errorlint
    - fatcontext
    - godot
    - ineffassign
    - makezero
    - misspell
    - nilerr
    - predeclared
    - staticcheck
    - unconvert
    - unused
    - govet
  disable:
    - errcheck
  settings:
    misspell:
      # UniFi's own wire field is spelled igmp_supression; the hand-written
      # encoder must emit it verbatim to match the controller (the generated
      # struct carries the same spelling). Not our typo to fix.
      ignore-rules:
        - supression
  exclusions:
    generated: lax
formatters:
  enable:
    - gci
    - gofmt
    - gofumpt
    - goimports
    - golines
  settings:
    gofmt:
      simplify: true
      rewrite-rules:
        - pattern: "interface{}"
          replacement: "any"
        - pattern: "a[b:len(a)]"
          replacement: "a[b:]"
    golines:
      max-len: 200
  exclusions:
    generated: lax
